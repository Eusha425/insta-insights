name: Ping Streamlit App

on:
  schedule:
    - cron: '* * * * *'  # Every minute

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Streamlit App
        run: curl -L https://insta-insights.streamlit.app/
